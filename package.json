{
  "name": "kairo",
  "version": "0.1.0",
  "private": true,
  "description": "MCP server that closes the loop between code generation and verification",
  "scripts": {
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint"
  },
  "packageManager": "pnpm@10.33.0"
}
